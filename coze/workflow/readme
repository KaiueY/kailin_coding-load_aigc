# 复杂coze应用之工作流

## 心情应用


-根据你的心情（LLM）返回一段文字（AIGC生成文本）和图片（文生图）

- 为何要学习ai

    -AIGC改变了前端用户体验
        example:coze创建应用的时候 根据标题和描述就可以生成图标
        传统是上传图片
        文生文（ChatGPT）——>文生图（coze）

    - 工作流    
        - AI应用的流程定制 
        - 默认有开始、结束两个结束节点
        - 添加节点 
            大模型节点（未来可以换其他）
        - 引用变量 输入和输出
        - 生成对应的图片 文生图插件
- markdown语法
    ![text]{url}