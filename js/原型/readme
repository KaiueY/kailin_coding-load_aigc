# 原型 （显式原型）
    - 原型是函数自带的一个属性 prototype  它定义了构造函数创建的实例对象的公共祖先。通过构造函数创建的对象，可以继承修改函数原型上的属性
    - 实例对象隐式具有构造函数原型上的属性  是只读属性



# 原型链
    - v8引擎在查找对象属性时,如果没找到，就会顺着对象的隐式原型去找，如果没有找到，再顺着隐式原型的隐式原型继续往上找 直到null 

# 隐式原型
    对象上的__proto__ 属性 他等于构造函数的prototype

# 所有对象都有原型嘛
    不是的
    Object.create(null) 创建出来的对象，就没有原型。